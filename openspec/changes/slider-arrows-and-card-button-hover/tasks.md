## 1. Slider Navigation Buttons Fix

- [x] 1.1 Add CSS rules in `public/assets/css/custom.css` hiding Swiper `:after` pseudo-elements and styling `.swiper-button-prev-style-1` and `.swiper-button-next-style-1` circular buttons with primary brand hover colors.

## 2. Car Card Button Hover Micro-animations

- [x] 2.1 Add hover micro-animation rules in `public/assets/css/custom.css` for `.card-button .btn` and `.card-journey-small .btn-primary`.

## 3. Verification

- [x] 3.1 Verify slider navigation buttons display styled Flaticon arrows without Swiper text icon conflicts.
- [x] 3.2 Verify car card View buttons respond dynamically with scale and shadow elevation on hover.
