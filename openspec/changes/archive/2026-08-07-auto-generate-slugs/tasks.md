## 1. Resource Form Updates

- [x] 1.1 Update `BrandResource.php` to auto-generate `slug` from `name`.
- [x] 1.2 Update `CarTypeResource.php` to auto-generate `slug` from `name`.
- [x] 1.3 Update `CarResource.php` to auto-generate `slug` from `name`.
- [x] 1.4 Update `ServiceResource.php` to auto-generate `slug` from `title`.
- [x] 1.5 Update `BlogPostResource.php` to auto-generate `slug` from `title`.

## 2. Resource Table Updates

- [x] 2.1 Remove or hide `slug` column from `BrandResource.php` table.
- [x] 2.2 Remove or hide `slug` column from `CarTypeResource.php` table.
- [x] 2.3 Remove or hide `slug` column from `CarResource.php` table.
- [x] 2.4 Remove or hide `slug` column from `ServiceResource.php` table.
- [x] 2.5 Remove or hide `slug` column from `BlogPostResource.php` table.
