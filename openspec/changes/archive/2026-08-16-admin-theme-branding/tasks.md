## 1. Dynamic Admin Panel & Login Branding Integration

- [x] 1.1 Update `AdminPanelProvider.php` to integrate dynamic `Color::hex()` primary color palette with hex sanitization and `#70f46d` fallback
- [x] 1.2 Update `AdminPanelProvider.php` to integrate dynamic `brandName`, `brandLogo`, `darkModeBrandLogo`, `brandLogoHeight`, and `favicon`
- [x] 1.3 Verify dynamic styling and brand logo rendering on `/admin` and `/admin/login` routes and test dark/light mode compatibility
- [x] 1.4 Run PHP syntax checks and automated regression tests
