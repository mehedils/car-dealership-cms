## 1. Setup

- [x] 1.1 Install the `guava/filament-icon-picker` package via Composer.

## 2. Resource Implementation

- [x] 2.1 Update `AmenityResource` to replace the `TextInput` for `icon` with `IconPicker::make('icon')`.
- [x] 2.2 Update `ServiceResource` to replace the `TextInput` for `icon` with `IconPicker::make('icon')`.
- [x] 2.3 Update `WhyUsFeatureResource` to replace the `TextInput` for `icon` with `IconPicker::make('icon')`.
